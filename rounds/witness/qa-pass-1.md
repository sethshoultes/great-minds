# QA Pass 1 — Witness (Narrate CLI)

**QA Engineer:** Margaret Hamilton
**Date:** 2026-04-05
**Scope:** Requirements traceability — does each requirement have a corresponding deliverable?

---

## Overall Verdict: **BLOCK**

Critical naming inconsistencies and missing requirements must be addressed before ship.

---

## Issues Summary (Ranked by Severity)

### P0 — Ship Blockers

| # | Issue | Requirement | Evidence |
|---|-------|-------------|----------|
| 1 | **Package name mismatch** | PS-2 requires `narrate-cli` | `package.json` line 2: `"name": "witness-cli"` |
| 2 | **CLI binary name mismatch** | PS-3 requires `narrate` command | `package.json` line 7: `"witness": "./bin/witness.js"` |
| 3 | **Config file name mismatch** | CFG-1 requires `.narraterc.json` | `config.js` line 23: `'.narraterc.json'` — file name is correct BUT all user-facing messaging says "Witness" not "Narrate" |
| 4 | **Init output text wrong** | CMD-3 requires exactly 4 lines: "Narrate is watching." / hook path / changelog path / invitation | `init.js` lines 104-109: Says "Witness is watching." |
| 5 | **Attribution footer text wrong** | FMT-7 requires `*Narrated by Narrate — your code, in plain English*` | `changelog.js` line 12: `'*Generated by Witness (witness.dev)*'` |

### P1 — Must Fix Before Release

| # | Issue | Requirement | Evidence |
|---|-------|-------------|----------|
| 6 | **Model default mismatch** | CFG-2, AI-10 require `claude-haiku-4-5-20251001` | `config.js` line 9: `'claude-haiku-4-5-20251001'` — **PASS** (correct) |

### P2 — Should Fix

| # | Issue | Requirement | Evidence |
|---|-------|-------------|----------|
| 7 | Inconsistent branding throughout codebase | Multiple | All files reference "witness" instead of "narrate" |

---

## Detailed Requirements Verification

### Project Setup (PS-1 through PS-6)

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| PS-1 | Node.js ESM runtime, no build step | **PASS** | `package.json` line 4: `"type": "module"`, no build tools in dependencies |
| PS-2 | npm package name: `narrate-cli` | **FAIL** | `package.json` line 2: `"name": "witness-cli"` |
| PS-3 | CLI binary command: `narrate` | **FAIL** | `package.json` lines 6-8: `"witness": "./bin/witness.js"` |
| PS-4 | Dependency: `@anthropic-ai/sdk` | **PASS** | `package.json` line 25: `"@anthropic-ai/sdk": "^0.82.0"` |
| PS-5 | Git via native `child_process` | **PASS** | `git.js` line 1: `import { execSync } from 'child_process'`, no simple-git |
| PS-6 | `package.json` with `"type": "module"`, `bin`, `engines` | **PASS** | `package.json` lines 4, 6-8, 9-11 |

### Core Hook (CH-1 through CH-7)

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| CH-1 | Post-commit reads `git diff HEAD~1 HEAD` + message | **PASS** | `git.js` line 22: `'git diff HEAD~1 HEAD'`; `hook-worker.js` lines 25-26 |
| CH-2 | Sends diff + message to Claude API with system prompt | **PASS** | `summarize.js` lines 69-84: API call with system prompt |
| CH-3 | Appends entry to `CHANGELOG.human.md` | **PASS** | `changelog.js` line 7: `'CHANGELOG.human.md'`; `hook-worker.js` line 62 |
| CH-4 | Runs as detached child process | **PASS** | `hook-runner.js` lines 20-28: `detached: true`, `child.unref()` |
| CH-5 | Hook returns < 50ms | **PASS** | `init.js` lines 29-32: spawns async with `&`; `hook-runner.js` spawns detached |
| CH-6 | Max diff size: 500 lines hardcoded | **PASS** | `summarize.js` line 20: `const MAX_DIFF_LINES = 500` |
| CH-7 | Truncates diffs > 500 lines | **PASS** | `summarize.js` lines 28-38: `truncateDiff` function |

### CLI Commands (CMD-1 through CMD-8)

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| CMD-1 | `narrate init` installs post-commit hook | **PASS** | `init.js` lines 65-100: writes to `.git/hooks/post-commit` |
| CMD-2 | Init detects existing hooks, appends | **PASS** | `init.js` lines 75-97: checks `existsSync`, appends with markers |
| CMD-3 | Init output exactly 4 lines | **FAIL** | `init.js` lines 103-110: Says "Witness" not "Narrate"; 6 console.log calls not 4 |
| CMD-4 | Init < 2 seconds | **PASS** | Sync file operations only, no API calls |
| CMD-5 | `narrate log` pretty-prints (no raw markdown) | **PASS** | `log.js` lines 117-124: ANSI formatting, no markdown symbols |
| CMD-6 | `narrate log --since=<date>` | **PASS** | `log.js` lines 32-69, 97-100: `parseSince` function, filters by date |
| CMD-7 | `narrate backfill --last=90d` | **PASS** | `backfill.js` line 100: `flags.last || '90d'` |
| CMD-8 | Backfill cost preview + confirmation | **PASS** | `backfill.js` lines 131-141: `estimateCost`, `confirm` prompt |

### Configuration (CFG-1 through CFG-6)

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| CFG-1 | Config file: `.narraterc.json` | **PASS** | `config.js` line 23: `'.narraterc.json'` |
| CFG-2 | Field `model` default `claude-haiku-4-5-20251001` | **PASS** | `config.js` line 9: `'claude-haiku-4-5-20251001'` |
| CFG-3 | Field `ignore` array, default `[]` | **PASS** | `config.js` line 10: `ignore: []` |
| CFG-4 | Field `attribution` boolean, default `true` | **PASS** | `config.js` line 11: `attribution: true` |
| CFG-5 | API key from `ANTHROPIC_API_KEY` env only | **PASS** | `config.js` lines 53-55: `process.env.ANTHROPIC_API_KEY` |
| CFG-6 | No `tone`, `changelog`, `maxDiffLines` in config | **PASS** | `config.js` lines 8-12: only 3 fields |

### Changelog Format (FMT-1 through FMT-7)

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| FMT-1 | Date format: `Apr 5, 2026 — 7:36 AM` | **PASS** | `changelog.js` lines 21-34: `formatDate` produces exact format |
| FMT-2 | Entry: sentence first, hash last with ` · ` | **PASS** | `changelog.js` line 58: `${cleanSummary}  ·  ${shortHash}` |
| FMT-3 | Entry indented 2 spaces | **PASS** | `changelog.js` line 58: `  ${cleanSummary}` |
| FMT-4 | Blank line between date/entry, entries | **PASS** | `changelog.js` line 58: `\n\n` separators |
| FMT-5 | Terminal rendering no raw markdown | **PASS** | `log.js` lines 117-124: strips formatting, uses ANSI |
| FMT-6 | File path: `CHANGELOG.human.md` hardcoded | **PASS** | `changelog.js` line 7: `'CHANGELOG.human.md'` |
| FMT-7 | Attribution footer when enabled | **FAIL** | `changelog.js` line 12: Wrong text — says "Generated by Witness" not "Narrated by Narrate" |

### System Prompt / AI (AI-1 through AI-10)

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| AI-1 | Uses exact v1 system prompt | **PASS** | `summarize.js` lines 3-18: matches decisions.md prompt |
| AI-2 | Entries start with verb | **PASS** | `summarize.js` line 9: "Start with a verb" |
| AI-3 | Never "This commit/change" | **PASS** | `summarize.js` line 10: rule present |
| AI-4 | Use specific names from code | **PASS** | `summarize.js` line 11: rule present |
| AI-5 | Never invent behavior | **PASS** | `summarize.js` line 12: rule present |
| AI-6 | Unhelpful message handling | **PASS** | `summarize.js` lines 13-14: rule present |
| AI-7 | Test-only diffs noted | **PASS** | `summarize.js` line 15: rule present |
| AI-8 | Dependency updates noted | **PASS** | `summarize.js` lines 16-17: rule present |
| AI-9 | Under 20 words | **PASS** | `summarize.js` line 18: rule present |
| AI-10 | Default model `claude-haiku-4-5-20251001` | **PASS** | `config.js` line 9 |

### Offline Fallback (OFF-1 through OFF-5)

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| OFF-1 | Activates when no API key or unreachable | **PASS** | `hook-worker.js` lines 40-60: checks `apiKey`, catches errors |
| OFF-2 | Produces rule-based sentence (not file list) | **PASS** | `fallback.js` lines 203-253: generates sentences like "Updated X and Y" |
| OFF-3 | Grammatical using diff headers + message | **PASS** | `fallback.js` lines 126-254: constructs proper sentences |
| OFF-4 | Good example format achieved | **PASS** | `fallback.js` line 216: `Updated ${file}` pattern |
| OFF-5 | Bad format avoided | **PASS** | No "Modified N files:" pattern anywhere |

### Quality / Performance (QA-1 through QA-6)

| ID | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| QA-1 | `narrate init` < 2 seconds | **PASS** | Sync operations only |
| QA-2 | Post-commit < 50ms return | **PASS** | Detached spawn with `&` |
| QA-3 | No hallucinated file names | **PASS** | Fallback uses actual diff headers |
| QA-4 | Works with repos of any size | **PASS** | 10MB buffer in `git.js` line 25 |
| QA-5 | Valid markdown, human-editable | **PASS** | Format is clean markdown |
| QA-6 | Installs on macOS, Linux, Windows | **PASS** | Pure Node.js, no native deps |

---

## Summary

**Total Requirements:** 52
**PASS:** 47
**FAIL:** 5

### Action Items for Development Team

1. **[P0]** Rename package from `witness-cli` to `narrate-cli` in `package.json`
2. **[P0]** Rename binary from `witness` to `narrate` in `package.json` bin field
3. **[P0]** Update init output to say "Narrate is watching." and match exact 4-line format
4. **[P0]** Update attribution footer to `*Narrated by Narrate — your code, in plain English*`
5. **[P2]** Consider global search/replace of "Witness" branding to "Narrate" across all files

---

*QA Pass 1 complete. Awaiting fixes before Pass 2.*
